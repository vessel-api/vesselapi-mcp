// Version advertised by the McpServer handshake. package.json and server.json
// carry their own copies and are NOT derived from this — keep all three in step
// when bumping.
export const VERSION = "2.0.0";
